interface:
  display_name: "Mermail Support Agent"
  short_description: "Triage, reply, escalate, and close support email"
  default_prompt: "Use $mermail-support-agent to triage this support inbox and draft a reply for approval."
dependencies:
  tools:
    - type: "mcp"
      value: "mermail"
      description: "Mermail workspace and mailbox MCP server"
      transport: "streamable_http"
      url: "https://console.mermail.app/mcp"
